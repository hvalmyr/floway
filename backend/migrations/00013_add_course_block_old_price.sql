-- +goose Up
-- NULL = нет скидки (обычная цена). Указывается, когда для блока действует
-- цена "было/стало" (см. блок "маленькие группы"/"о школе" на сайте про
-- скидку 10% при раннем бронировании) — до этой миграции поле существовало
-- только во фронтенд-моках, реальные данные его никогда не заполняли.
ALTER TABLE course_blocks ADD COLUMN old_price INTEGER;

-- +goose Down
ALTER TABLE course_blocks DROP COLUMN old_price;
